
export default function postData(joke,emailList) {
/* SERVICE NOT IMPLEMENTED */
/*
    const data = {
            joke : joke,
            emails : emailList
        }
    fetch("/sendEmail", {
        method: 'POST',
        headers: {
            'Content-Type': 'application/json'
        },
        body: JSON.stringify(data)
    })
    
    .then(res => console.log(res))
*/
    return
}
